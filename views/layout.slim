doctype html
html
  head
    meta charset="utf-8"
    title Tweet Deleter
  body
    #wrapper
      - if flash[:notice]
        .notice
          p = flash[:notice]
      - if flash[:error]
        .error
          p = flash[:error]
      #content
        == yield
